<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateUsersTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('users', function (Blueprint $table) {
            $table->id();
            $table->string('avatar')->nullable();
            $table->string('name');
            $table->string('email')->unique();
            $table->timestamp('email_verified_at')->nullable();
            $table->string('password');
            $table->integer('group_id')->default(0);
            $table->integer('currency_id')->default(15);
            $table->string('language')->default('pt-BR');
            $table->string('theme')->default('sidebar-mini layout-fixed control-sidebar-slide-open sidebar-mini-md sidebar-mini-xs layout-navbar-fixed text-sm sidebar-collapse');
            $table->integer('timezone_id')->default(179);
            $table->rememberToken();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('users');
    }
}
